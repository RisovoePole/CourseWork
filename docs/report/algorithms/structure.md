Структура описания одного алгоритма
1. Идея и происхождение

Откуда алгоритм (природная метафора, область происхождения), какую задачу изначально решал, почему подходит для NP-трудных комбинаторных задач типа расписания.
2. Формальная модель / ключевые компоненты

Базовый словарь понятий алгоритма:

3. Алгоритм работы (псевдокод / блок-схема)

Пошаговый цикл итерации — инициализация → основной цикл → критерий останова. 
4. Применение к задаче составления расписания

Как кодируется решение (representation) — прямое/непрямое (direct/indirect encoding)
Как учитываются жёсткие ограничения (hard constraints)
Как учитываются мягкие ограничения (soft constraints) 

5. Сложность и эффективность

Вычислительная сложность одной итерации
Поведение при росте размерности задачи (число групп/аудиторий/преподавателей)
Скорость сходимости, риск застревания в локальном оптимуме / способы его избежать

6. Преимущества и недостатки

 список плюсов/минусов именно в контексте scheduling (не абстрактно).
7. Примеры использования в существующих исследованиях/системах

1-2 ссылки на статьи или реальные тимтейблинг-системы, использующие этот алгоритм (для библиографии).

8. Вывод по алгоритму